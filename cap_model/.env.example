# MLflow Configuration
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT_NAME=card-approval-classification

# GCP Configuration (if using GCS for artifacts)
GCP_PROJECT_ID=product-recsys-mlops
GCS_BUCKET=product-recsys-mlops-recsys-data

# Model Configuration
MODEL_TYPE=xgboost
RANDOM_SEED=42

# Database (optional - for production)
# DATABASE_URL=postgresql://user:password@localhost:5432/card_approval

# API Configuration (for future deployment)
# API_HOST=0.0.0.0
# API_PORT=8000
